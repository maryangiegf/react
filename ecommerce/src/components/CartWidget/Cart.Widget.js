import images from "./assets/images.png"

const CartWidget = () => {
    return (
        <div>
        <img src = {images} alt = "cart-widget" />
        0
        </div>
    )
}

export default CartWidget